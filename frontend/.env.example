# NOTE: The real single source of truth is ../credentials/urls.json (GATEWAY_URL).
# vite.config.js reads that file automatically at build/dev time.
# This .env is only a manual override/fallback if you can't reach the parent folder.
VITE_GATEWAY_URL=http://localhost:5000
